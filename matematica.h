#pragma once
#include <cmath>

/*
Função calcula seno do ângulo em radiano.
*/
float calcular_seno(float angulo_radianos);

/*
Função calcula cosseno do ângulo em radiano.
*/
float calcular_cosseno(float angulo_radianos);

/*
Função calcula tangente do ângulo em radiano.
*/
float calcular_tangente(float angulo_radianos);